############################################################
# macOS SYSTEM FILES
############################################################
# These files are automatically created by macOS Finder.
# They contain folder view settings and metadata unique to your local machine.
# Safe to ignore in all projects.
.DS_Store
.AppleDouble
.LSOverride

# Icon file that sometimes appears in folders
Icon

# Spotlight & Trash — appear when using external drives or network folders
.Spotlight-V100
.Trashes

############################################################
# XCODE PROJECT FILES
############################################################
# Xcode creates user-specific settings and derived data when building.
# These are local to your environment and should not be shared.
DerivedData/
xcuserdata/

# Session-specific state data — avoid polluting commits
*.xcuserstate

# Xcode workspace and scheme management
# Safe to ignore if automatically generated.
# Keep if your team uses shared schemes or workspaces intentionally.
*.xcscmblueprint
*.xccheckout

# Optional: ignore user data inside workspace or project files
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/

############################################################
# BUILD OUTPUT & APP PACKAGING
############################################################
# iOS build artifacts — can be large, automatically generated by Xcode.
*.ipa          # Exported app package for distribution
*.dSYM.zip     # Debug symbol archives
*.dSYM         # Symbol files for crash reports
*.xcarchive    # Xcode archive bundles (e.g., when exporting for TestFlight)

############################################################
# PLAYGROUNDS
############################################################
# These are automatically generated for Swift Playgrounds.
# Usually not needed in source control.
timeline.xctimeline
playground.xcworkspace

############################################################
# SWIFT PACKAGE MANAGER
############################################################
# The build output and local package metadata are auto-generated.
.build/
.swiftpm/

# Keep Package.swift and Package.resolved if using dependency pinning.
# - If you want reproducible builds, commit Package.resolved.
# - If dependencies change often, you may ignore it.
!Package.resolved

############################################################
# COCOAPODS (OPTIONAL)
############################################################
# If your project uses CocoaPods:
# - Ignoring Pods/ means each developer runs `pod install` locally.
# - Committing Pods/ makes builds deterministic but bloats the repo.
# Choose based on your team's workflow.
# Pods/
# *.xcworkspace

############################################################
# CARTHAGE (OPTIONAL)
############################################################
# Carthage builds frameworks locally.
# Checkouts can be ignored, but you may keep binary builds if shared.
Carthage/Build/
# Carthage/Checkouts

############################################################
# FASTLANE (OPTIONAL)
############################################################
# Fastlane generates reports, screenshots, and logs.
# These files are temporary and should not be committed.
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output/

############################################################
# LOGS, SECRETS, AND ENV FILES
############################################################
# Common temporary and sensitive files
*.log
*.env          # Environment variables (API keys, secrets)
*.key
*.mobileprovision  # Apple provisioning profiles (should be stored securely)

############################################################
# TEMPORARY & CACHE FILES
############################################################
# General system temp files, editor swap files, and backups
*.swp
*.swo
*.tmp
*.temp
*.bak
*~

############################################################
# CODE COVERAGE / REPORTS
############################################################
# Build coverage data or profiling reports — generated by Xcode or CI tools
*.gcda
*.gcno
*.profraw
*.profdata
coverage/
report/

############################################################
# SWIFTLINT / FORMATTER
############################################################
# Usually safe to commit since these define project-wide rules.
# Ignore only if you use personal overrides.
# .swiftlint.yml
# .swiftformat

############################################################
# LOCAL DEVELOPMENT ARTIFACTS
############################################################
# Files related to local development or CI setup
*.plist.lock
.xcodeproj.zip
.project-local/

